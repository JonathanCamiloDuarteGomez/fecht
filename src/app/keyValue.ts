// Definimos un tipo para el par clave-valor

// Definimos un tipo para el par clave-valor
export type KeyValue = [number, {number: number, string?: string}]; // any para permitir cualquier tipo de valor

// Creamos un array de pares clave-valor
